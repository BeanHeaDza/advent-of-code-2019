const nSquareMap = require("./n-square-map");
const permutations = require("./permutations");
const { readFile, readContent } = require("./read-file");
const { combinations, combinationsGenerator } = require("./combinations");

module.exports = {
  nSquareMap,
  permutations,
  readFile,
  readContent,
  combinations,
  combinationsGenerator
};
